# MTG3DS

Build after installing devkitpro by running:

```bash
cargo 3ds build
```

It can be run by pressing `y` in homebrew launcher and entering the IP in:

```bash
cargo 3ds run --address <IP_ADDR>
```
